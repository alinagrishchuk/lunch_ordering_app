%h1 My orders

%table.table.table-bordered.table-striped.table-hover
  %thead
    %tr
      %th Date
      %th Product
      %th Course type
      %th Price
  %tbody
    - @orders.each do |order|
      - order.order_products.each do |order_product|
        %tr
          %td= order.date
          %td= order_product.menu.name
          %td= order_product.menu.course_type
          %td= order_product.menu.price
