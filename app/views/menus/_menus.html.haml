.row
  .col-sm-10.col-sm-offset-1
    %table.table.table-bordered.table-striped.table-hover
      %thead
        %tr
          %th Product
          %th Price
      %tbody
        - @course_types.each do |_,course_type|
          -if @menus.key? course_type
            %tr
              %td.warning{colspan:2}=course_type
              - @menus[course_type].each do |menu|
                %tr
                  %td= menu.name
                  %td= menu.price
